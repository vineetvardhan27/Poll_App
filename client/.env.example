# Backend API URL (Update this with your Render URL after backend deployment)
VITE_API_URL=http://localhost:5000/api

# Socket.IO Server URL (Update this with your Render URL after backend deployment)
VITE_SOCKET_URL=http://localhost:5000
